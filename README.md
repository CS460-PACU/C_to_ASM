# C to ASM Example

## Build the example:

```make```

## View the assembly code:

```less bin/test.asm```

## View the object file:

```objdump -d bin/test.o```

## View the executable file:
```objdump -d bin/test```
